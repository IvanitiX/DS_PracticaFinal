<%= form_with(model: tecnico) do |form| %>
  <% if tecnico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tecnico.errors.count, "error") %> prohibited this tecnico from being saved:</h2>

      <ul>
        <% tecnico.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :apellidos %>
    <%= form.text_field :apellidos %>
  </div>

  <div class="field">
    <%= form.label :contrasena %>
    <%= form.text_field :contrasena %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <!-- Vamos a hacer un desplegable -->
  <div class="field">
	<%= form.label :tipo_tecnico %>
	<%= form.collection_select :tipo_tecnico, TipoTrabajo.all, :Tipo, :Tipo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
